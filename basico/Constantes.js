//Uso y declaracion de constantes en JS

// Forma de definir una constante 
//Solo se puede definir como mayuscualas
const N1 = 3.1;

//Al ser constante no se puede modificar a lo largo del programa.

console.log(N1);

// Acceso de constante de pi con la libreria de math 
console.log(Math.PI);

