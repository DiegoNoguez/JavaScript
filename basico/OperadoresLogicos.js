//operadoes logicos de js 
/*
operador and && 
operador or || 
operaodor not !
*/ 

//Ejemplos de operadores logicos 

let a = true;
let b = false;

console.log(a)
console.log(b)

//and
 
console.log(`${a} && ${b} -> ${a && b}`);

// or 
console.log(`${a} || ${b} -> ${a || b}`);

// not !
console.log(`${a} -> ${!a}`);